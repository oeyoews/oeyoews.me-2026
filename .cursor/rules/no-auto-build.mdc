---
description: 不要主动构建/启动/安装；只有在用户明确要求时才运行命令
alwaysApply: true
---

# 不要主动构建项目

- 默认只进行阅读、分析与代码修改。
- **除非用户明确要求**，否则不要主动执行任何会构建/运行/安装/影响环境的命令，例如：
  - `pnpm|npm|yarn|bun install`
  - `pnpm|npm|yarn|bun run dev|build|preview|test`
  - `vite dev|build|preview`
- 如果需要运行命令来验证改动，必须先在回复里说明要运行什么命令以及目的，并等待用户明确同意后再执行。

